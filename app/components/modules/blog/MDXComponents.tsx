import React from 'react';

// Minimal MDX components - let the Content styled component handle the styling
const MDXComponents = {
  // Keep default HTML elements - styling is handled by the Content component
};

export default MDXComponents;

